{% extends "base.html.twig" %}
{% block head %}
<title>Arras games</title>
<link rel="icon" href="assets/images/icon.ico">
{% endblock %}
{% block body %}
<div class="body_bg">
{# 

SECTION @HEADER

 #}
{% include "partials/_header.html.twig" %}
{# 

SECTION @BANNIERE

 #}
<section class="banner_part">
    <div class="container">
        <div class="row align-items-center justify-content-between">
            <div class="col-lg-6 col-md-8">
                <div class="banner_text">
                    <div class="banner_text_iner">
                        <h1>Parlons de nous</h1>
                        <p>Arras Cyber Game, situé au centre de la ville d'arras, existe depuis aujourd'hui 1203 ans</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{# 

SECTION @APPLICATION

 #}
<section class="about_us section_padding">
    <div class="container">
        <div class="row align-items-center justify-content-between">
            <div class="col-md-5 col-lg-6 col-xl-6">
                <div class="learning_img">
                    <img src="assets/img/about_img.png" alt="">
                </div>
            </div>
            <div class="col-md-7 col-lg-6 col-xl-5">
                <div class="about_us_text">
                    <h2>Accédez à votre plateforme de jeux en toute simplicité</h2>
                    <p>Saisissez votre jeton d’authentification pour déverrouiller l’accès à l’ensemble de votre bibliothèque de jeux. Installez l’application et profitez d’une expérience fluide et sécurisée. </p>
                    <a href="#" class="btn_1">Installer</a>
                </div>
            </div>
        </div>
    </div>
</section>
{# 

SECTION @FOOTER

 #}
{% include "partials/_footer.html.twig" %}


</div>

{# {% block stylesheet %}
<style>
.grid-data {
    width: 150px; 
    height:150px; 
    margin:35px;
    color: white;
    font-weight:bold;
}
</style>
{% endblock %} #}


{# <div class="container">
<h1>Réservations</h1>
  <div class="row">
  {% if machines %}
    {% for machine in machines %}
          {% if machine.state != 1 %}
              <div class="col-8 bg-primary d-flex justify-content-center grid-data flex-column">
                  <p>{{machine.name}}</p>
                  <p>{{machine.games}}</p>
                  <a href="{{path('machines.reservation', {id: machine.id})}}" class="btn btn-info">Réserver</a>
              </div>
          {% endif %}
    {% endfor %}
  {% else %}
    <p>Il n'y a pas de machine disponible</p>
  {% endif %}
  </div>
</div> #}

{% endblock %}